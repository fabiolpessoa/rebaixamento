services:
  web:
    build: ./backend
    container_name: rebaixamento_django

    volumes:
      - ./backend:/app

    ports:
      - "8000:8000"

    env_file:
      - .env
    
    dns:
      - 8.8.8.8
      - 1.1.1.1